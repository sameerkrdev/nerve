root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/matching-engine.exe ./cmd/matching-engine"
bin = "tmp/matching-engine.exe"

include_ext = ["go"]

include_dir = [
  ".",
  "../../packages/proto-defs/go/generated"
]

exclude_dir = [
  "tmp",
  "vendor",
  "node_modules"
]

delay = 200

[log]
time = true
