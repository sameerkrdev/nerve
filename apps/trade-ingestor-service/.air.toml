root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/trader-ingestor-service ./cmd/trader-ingestor-service"
bin = "tmp/trader-ingestor-service"

include_ext = ["go"]

include_dir = [
  ".",
  "../../packages/proto-defs/go/generated"
]

exclude_dir = [
  "tmp",
  "vendor",
  "node_modules"
]

delay = 200

[run]
cmd = "./tmp/trader-ingestor-service"

[log]
time = true
